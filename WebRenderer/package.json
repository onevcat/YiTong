{
  "name": "yitong-web-renderer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "vitest run",
    "licenses": "node scripts/generate-third-party-licenses.mjs",
    "licenses:check": "node scripts/generate-third-party-licenses.mjs --check",
    "copy-assets": "node scripts/copy-assets.mjs",
    "bundle-assets": "npm run build && npm run copy-assets && npm run licenses"
  },
  "dependencies": {
    "@pierre/diffs": "1.2.12"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "7.3.1",
    "vitest": "^4.0.18"
  }
}
